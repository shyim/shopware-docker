#!/usr/bin/env bash

if [[ -e /nvm/versions/ ]]; then
  rm -rf /nvm/*
  rm -rf /nvm/.cache
fi

if [[ ! -e /nvm/fmn/ ]]; then
  fnm --fnm-dir=/nvm/.fnm install 12
  eval $(fnm --fnm-dir=/nvm/.fnm env --shell=bash)
  fnm use 12
else
  eval $(fnm --fnm-dir=/nvm/.fnm env --shell=bash)
fi